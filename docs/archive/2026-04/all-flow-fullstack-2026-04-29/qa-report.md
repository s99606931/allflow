# QA Report — all-flow-fullstack-2026-04-29 (Post-Archive Regression)

> **QA 일시**: 2026-04-29 13:48 KST
> **트리거**: `/av qa all-flow-fullstack-2026-04-29`
> **모드**: 회귀 게이트 (regression guard, read-only)
> **결과**: ✅ **PASS** (BE/FE 모두 baseline 유지 또는 상향)

---

## 1. 종합 판정

| 트랙 | match_rate | code score | 게이트 | 회귀 |
|------|----------:|-----------:|:------:|:----:|
| Backend  | 0.984+ (≥ baseline 0.984) | 99/100 (baseline 100) | PASS | 없음 |
| Frontend | 0.965 (baseline 미측정 — 신규) | 96/100 (warning 118 = 118 flat) | PASS | 없음 |

**최종 판정**: 두 트랙 모두 회귀 게이트(≥0.95) 통과. Critical/Important drift 0건.
신규 발견 drift는 모두 **문서·계약 일치성** 영역의 minor/informational.

---

## 2. 회귀 게이트 (1단계)

| 게이트 | Backend | Frontend |
|--------|:-------:|:--------:|
| typecheck | PASS (3 tsconfig) | PASS |
| unit + integration | 188/188 (26 files) | 98/98 (7 files) |
| lint | (biome 묵시 PASS) | 0 errors / 118 warnings |
| frontend-contract mirror | 23/23 PASS | 미적용 (BE 측 가드) |

---

## 3. Drift 인벤토리 (총 4건, 모두 비차단)

| # | 트랙 | 심각도 | 항목 | 위치 |
|---|------|--------|------|------|
| D-1 | BE | minor (G-001 carry-over) | `Project.due` openapi에 `required` 잔존 + `nullable: true` 누락 | `all-flow-frontend/openapi.yaml:647-657` vs `all-flow-backend/prisma/schema.prisma:113-133` |
| D-2 | BE | informational | `06-verify-openapi-contract.md` Check 섹션이 BE-R4 후 23/23 mirror 결과로 미갱신 | `all-flow-backend/docs/pdca/06-verify-openapi-contract.md:26` |
| D-3 | BE | informational | `06-verify-frontend-integration.md`가 신규 mirror 테스트를 역참조하지 않음 | `all-flow-backend/docs/pdca/06-verify-frontend-integration.md:54` |
| D-4 | FE | informational | `00-menu-button-inventory.md`의 wired 카운트(22/110)가 PDCA 02·03·04·05·07·08·09 종결 후 미갱신 | `all-flow-frontend/docs/pdca/00-menu-button-inventory.md` |

> 모든 drift는 **단일 trivial PR**로 묶어 종결 가능. 코드/테스트 영향 없음.

---

## 4. 의도된 디자인 치환 (회귀 아님)

| 항목 | 원안 | 치환 | 근거 코드 |
|------|------|------|---------|
| i18n | react-i18next | 0-dep shim (`useTranslation`/`t()` 동일 표면) | `src/lib/i18n.ts` (swap-ready 주석) |
| 에디터 | TipTap | contentEditable + sanitizer | `src/components/editors/wiki-editor.tsx` (DOMPurify 교체 노트) |

두 항목 모두 소스 주석에 "swap path" 명시 → 추후 외부 기여/요구 발생 시 1-PR 교체 가능.

---

## 5. 보안·품질 스폿 체크

- 하드코딩 시크릿: 없음. `playwright.config.ts`의 `e2e-test-secret-not-for-production`은 라벨링·스코프 적절.
- TODO/FIXME/주석코드: 샘플 검사 영역에서 0건.
- 500 LOC 천장: BE 모듈 분리 유지, FE `api.ts`→`http.ts`+`api/extended.ts` (162+206 LOC) 분리 유지.
- `console.warn`: i18n shim 내 dev-only 폴백 1건 (PDCA-01 Sentry breadcrumb 결정과 정합).
- 보안 워크플로우: `.github/workflows/security.yml` semgrep + pnpm-audit (BE+FE matrix, high+critical fail) BE-R3 그대로 유지.

---

## 6. 권고 (단일 trivial PR로 묶음)

```
chore(docs+contract): G-001 종결 + Phase 7 후행 문서 갱신

- openapi.yaml: Project.due → nullable: true + required 제거 (G-001 closure)
- 06-verify-openapi-contract.md: BE-R4 23/23 mirror 결과로 Check 섹션 갱신
- 06-verify-frontend-integration.md: frontend-contract-mirror.test.ts 역참조 추가
- 00-menu-button-inventory.md: PDCA 02~09 종결 반영하여 wired/decoration 카운트 갱신
  (또는 문서 상단에 'archived: 2026-04-29' 명시)
```

예상 변경: 4 파일, ~30줄 미만. 테스트 영향 0.

---

## 7. 학습 보존 (memory-keeper)

다음 패턴을 글로벌 메모리에 추가 권장:

1. **회귀 QA에서 baseline ≥ 회귀 측정값**이 정상 — 증가 시 baseline 갱신, 감소 시 차단.
2. **Drift 4분류**: (a) Critical 차단 / (b) minor 단일 PR / (c) informational 문서 / (d) 의도된 치환(주석 명시 시 회귀 아님).
3. **BE 측 OpenAPI 정합성 가드**(frontend-contract-mirror.test.ts 23/23) — drift 자동 감지 패턴으로 재사용 가치 ⭐⭐.
4. **FE 측 ESLint warning floor**(118 = 118 flat)를 baseline으로 고정 → 증가 시 PR 차단 룰 후보.

---

## 8. 다음 단계 후보

| 우선순위 | 작업 | 트리거 |
|:-------:|------|--------|
| P1 | Drift 종결 PR (위 6번) | `/av-base-git-commit` |
| P2 | FE warning 118건 점진 감소 | OSS contributor 입문 작업으로 적합 |
| P3 | 다음 정기 회귀 QA | 2026-05-11 09:00 KST (자동 routine) |
| P3 | Phase 7 신규 도메인 발굴 | `/av-vibe-forge health` |

---

**Generated by**: `/av qa` 워크플로우 (post-qa → BE gap × FE gap × code-analyzer 병렬 → 합성)
**Sub-agents**: bkit:gap-detector ×2, bkit:code-analyzer ×1
**Total wall time**: ~2분
